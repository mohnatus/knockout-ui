/**
 * c-dropdown
 *
 * Выпадающая плашка с контентом
 *
 * parent {string}
 * target {string} - id
 * open {observable<boolean>}
 * dropdownParams {Object}
 * modal {observable<boolean>}
 * className {string}
 */

import { ViewModel as model } from "./model";
import { template } from "./template";
import "./style.less";

export { model, template };
